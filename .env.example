# RootLedger — optional. v2 live world briefing works with public APIs and no keys.
PORT=8000
VITE_API_URL=http://127.0.0.1:8000

# Nominatim requires a contactable User-Agent (OSM usage policy).
NOMINATIM_USER_AGENT=RootLedger-HackMIT/1.0 (live-prevention; educational)

# NASA FIRMS MAP_KEY is optional; v2 uses NASA EONET wildfires by default.
# FIRMS_MAP_KEY=
