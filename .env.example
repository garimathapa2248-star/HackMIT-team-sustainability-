PORT=8000
VITE_API_URL=http://127.0.0.1:8000

# Optional. Ask tab uses this free OpenRouter model with artifact tools.
# Without a key, Ask uses the grounded regex agent (no LLM).
# Get a key at https://openrouter.ai/keys — :free models are $0.
OPENROUTER_API_KEY=
ROOTLEDGER_MODEL=openrouter/free
